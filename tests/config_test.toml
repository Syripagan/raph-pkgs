[package]
name = "config_test"
version = "2.12.1"
description = "this is for my tests dont touch"
source = "https://ftp.gnu.org/gnu/hello/hello-2.12.1.tar.gz"

[build]
system = "manual"
build_commands = [
	"echo multilib: $RAD_MULTILIB",
	"echo makeopts: $RAD_MAKEOPTS"
]
install_command = "true"
